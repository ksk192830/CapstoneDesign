# Laptop Client

Laptop-side application for receiving camera streams, running vision analysis,
generating control commands, and sending commands back to the ESP32-P4.

Run from the `laptop` directory:

```bash
python -m machine_vision_client.main
```

